apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  labels:
    app: booking-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      containers:
        - name: booking-service
          image: booking-service:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8094
              name: http
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
            - name: SERVER_PORT
              value: "8094"
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: "host.docker.internal:9094,host.docker.internal:9095"
            - name: OPENSEARCH_HOST
              value: "host.docker.internal"
            - name: OPENSEARCH_PORT
              value: "9200"
            - name: POSTGRES_HOST
              value: "host.docker.internal"
            - name: POSTGRES_PORT
              value: "5432"
            - name: KEYCLOAK_ISSUER_URI
              value: "http://kubernetes.docker.internal:8082/realms/ticketmaster"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8094
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 10
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8094
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: booking-service
  labels:
    app: booking-service
spec:
  selector:
    app: booking-service
  ports:
    - name: http
      port: 8094
      targetPort: 8094
  type: ClusterIP
---
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: booking-service
spec:
  host: booking-service
  trafficPolicy:
    loadBalancer:
      simple: ROUND_ROBIN


