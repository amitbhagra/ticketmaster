version: '3.8'

services:
  keycloak:
    image: quay.io/keycloak/keycloak:latest
    container_name: keycloak_server
    command: start-dev
    environment:
      # Keycloak Bootstrap admin variables
      - KC_BOOTSTRAP_ADMIN_USERNAME=admin
      - KC_BOOTSTRAP_ADMIN_PASSWORD=admin
      # Database Connections
      - KC_DB=postgres
      - KC_DB_URL_HOST=postgres
      - KC_DB_URL_DATABASE=keycloak
      - KC_DB_USERNAME=keycloak
      - KC_DB_PASSWORD=password
      # Force a stable issuer URL (not request-host derived)
      - KC_HTTP_ENABLED=true
      - KC_PROXY_HEADERS=xforwarded
      - KC_HOSTNAME=http://kubernetes.docker.internal:8082
      - KC_HOSTNAME_ADMIN=http://kubernetes.docker.internal:8082
      - KC_HOSTNAME_STRICT=true
      - KC_HOSTNAME_STRICT_HTTPS=false
    ports:
      - "8082:8080"
    depends_on:
      - postgres
    networks:
      - keycloak_network

  postgres:
    image: postgres:latest
    container_name: keycloak_db
    environment:
      - POSTGRES_DB=keycloak
      - POSTGRES_USER=keycloak
      - POSTGRES_PASSWORD=password
    volumes:
      # FIX: Mount directly to /var/lib/postgresql to comply with 18+ standards
      - postgres_data:/var/lib/postgresql
    networks:
      - keycloak_network

volumes:
  # Isolated volume definition
  postgres_data:

networks:
  keycloak_network:
    driver: bridge
